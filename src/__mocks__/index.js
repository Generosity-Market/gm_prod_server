const cause = require('./cause.mock');
const donations = require('./donations.mock');
const user = require('./user.mock');
const awsUtilsData = require('./awsUtilsData.mock');

module.exports = {
    awsUtilsData,
    cause,
    donations,
    user,
};

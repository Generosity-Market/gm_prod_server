// const { donations } = require('../../__mocks__');

// jest.mock('../../utilities/awsUtils');

// const {
//     createItem,
//     // editItem,
//     getAll,
//     getById,
//     login,
// } = require('../utils');

// const {
//     details,
//     editedDetails,
//     editedPreferences,
//     // images,
//     // Preferences,
// } = cause;

describe('\nDonation Endpoints\n', () => {
    // let response;

    describe('[POST] - /api/donations', () => {
        // beforeAll(async () => {
        //     // response = await createItem('donations', { user_id, ...details });
        // });

        it.todo('should return status code 201 (Created)');

        it.todo('should return a newly created donation');
    });
});
